{
  "name": "chat",
  "version": "1.0.0",
  "description": "Yandex practicum 18. Chat",
  "author": "",
  "license": "ISC",
  "source": "src/index.html",
  "scripts": {
    "start-dev": "parcel src/index.html --no-source-maps --dist-dir ./dist",
    "build": "parcel build src/index.html --no-source-maps --dist-dir ./dist",
    "start": "parcel src/index.html --open --port=3000",
    "server": "node ./server.js"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "targets": {
    "default": {
      "distDir": "dist",
      "engines": {
        "browsers": ">= 50%"
      }
    }
  },
  "engines": {
    "node": "16.16.0"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.6.2",
    "@parcel/transformer-sass": "^2.6.2",
    "parcel": "^2.6.2",
    "parcel-transformer-hbs": "^1.0.4"
  },
  "dependencies": {
    "express": "^4.18.1",
    "handlebars": "^4.7.7"
  }
}
